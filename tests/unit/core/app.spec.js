'use strict';

describe('config block', function() {
    beforeEach(function () {
        module('app');
        inject();
    });

    it('should run config blocks', function(done){
        done();
    });
});
